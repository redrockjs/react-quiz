import * as actions from './actionTypes';


export const rootReducer = (state, action) => {
  switch (action.type) {

    case actions.ANSWER_ADD:
      return state

    default:
      return state
  }
}
